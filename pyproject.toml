[project]
name = "email-triage-env"
version = "0.1.0"
description = "OpenEnv Email Classification Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openenv>=0.1.0",
    "openai"
]

[project.scripts]
server = "server.app:main"
