[project]
name = "email-triage-env"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:main"
